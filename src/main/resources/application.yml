spring:
  application:
    name: leadgen
  datasource:
    url: jdbc:postgresql://localhost:5432/leadgen
    username: leadgen
    password: changeme
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  mail:
    host: smtp.example.com
    port: 587
    username: changeme
    password: changeme
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY:}
      chat:
        options:
          model: claude-sonnet-4-6
          temperature: 0.7

places:
  api-key: ${PLACES_API_KEY:}
